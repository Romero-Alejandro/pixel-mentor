// Re-export schemas from auth.ts for backward compatibility
// The auth schemas are already defined in the main auth.ts file
// This index.ts is here for future schema-specific auth exports if needed
